// Week 1 exit criterion: prove the build -> test -> CI pipeline works
// before any physics code exists. Real tests replace/join this one
// starting Week 2 (math core).

#include <gtest/gtest.h>

TEST(Placeholder, Builds) {
  ASSERT_TRUE(true);
}
